      - name: 🏗️ Build Android APK via EAS
        # تمت إزالة علم --local ليتم البناء على سحابة Expo مباشرة
        run: pnpm dlx eas-cli build --platform android --profile preview --non-interactive
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
          
